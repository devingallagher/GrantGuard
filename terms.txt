
The Answer: B. SLE
 SLE (Single Loss Expectancy) describes the financial impact of 
a single event.


SMB : server message block port 445

FTP: File transfer Protocol port 21

SSh
smbclient is a Linux command-line tool that lets you interact with SMB shares, like a terminal-based FTP client for S


SMB stands for Server Message Block. It's a network file sharing protocol used mainly by Windows systems.

PORT 445

shares is a folder driver or something accessible over the network. coiuld be a printer

smbclient -L ip

this will list all shares than 

smbclient \\\\ip\\Workshares this means smbclient \\ip\WorkShares
password : whatever you want

🔍 -p- = Scan all 65,535 ports
Normally, Nmap only scans the 1,000 most common ports by default (when you don’t specify any -p flag).

But if you run:

bash
Copy
Edit
nmap -p- 10.10.10.10